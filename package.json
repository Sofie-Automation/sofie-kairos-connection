{
  "name": "sofie-kairos-connection",
  "license": "MIT",
  "packageManager": "yarn@4.9.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">= 20"
  },
  "scripts": {
    "build": "rimraf dist && run build:main",
    "build:main": "tsc --build --force",
    "build:watch": "run build:main --watch"
  },
  "devDependencies": {
    "@sofie-automation/code-standard-preset": "^3.0.0",
    "@types/node": "^22.15.29",
    "eslint": "^9.28.0",
    "prettier": "^3.5.3",
    "typescript": "~5.7.3"
  }
}
